import 'package:flutter/material.dart';

class EditDetailGroup extends StatelessWidget {
  const EditDetailGroup({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Ini Detail Group"),
    );
  }
}
